{
    "version": "2.0.0",
    "tasks":[
        {
            "label" : "build",
            "type" : "shell",
            "command" : "gcc",
            "args": ["-ID:${workspaceFolder}/","-g","-o","${fileBasenameNoExtension}.out","${file}" ]
        }
    ]
}