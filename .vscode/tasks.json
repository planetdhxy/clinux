{
    "version": "2.0.0",
    "tasks": [
        {
          "label": "build",
          "command": "make",
          "args": [],
          "options": {
            "cwd": "${workspaceFolder}/"
          }
        }
      ]
}